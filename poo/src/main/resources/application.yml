spring:
  security:
    enabled: false
    basic:
      enabled: false
  data:
    mongodb:
          uri: mongodb+srv://agusaviles26:${MONGO_DB_PASSWORD}@clusterapirestpoo.l7cqtpk.mongodb.net/?retryWrites=true&w=majority&appName=ClusterAPIRESTPOO
          database: ClusterAPIRESTPOO
  messages:
    basename: messages
    encoding: UTF-8

logging:
  level:
    root: info
management:
  security:
    enabled: false
  endpoints:
    enabled-by-default: false
    web:
      base-path: /metrics
      exposure:
        include: "health,info"
  endpoint:
    info:
      enabled: true
    health:
      enabled: true
       
poo:
  exceptions:
    code-prefix: 'UADER-POO'
  cors:
    allowed-origins: "*"
    allowed-methods: GET,POST,PUT,PATCH,DELETE,OPTIONS
    allowed-headers: "*"
    max-age: 3600
  
server:
  port :  ${CONECTOR_PORT:9001}
  servlet:
    contextPath: /poo
